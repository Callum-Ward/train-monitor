import React from "react";

export default function NewBoard({JsonData}) {

    return (
        <div>
            <table className="table table-sm">
                <>
            </table>
            <label typeof="text"></label>
        </div>
    );
}